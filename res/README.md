# Release assets

Replace the two empty ZIP archives in this folder with the final public release files while keeping the filenames unchanged:

- `omnicad_code.zip`
- `omnicad_samples.zip`

The download buttons in `index.html` already point to these paths.
