# Models

This directory is intentionally reserved as a placeholder.

The current OmniCAD site does **not** require GLB/GLTF assets: the qualitative section uses a single MP4 at `public/media/omnicad_qualitative.mp4`. Keep this folder only for future optional 3D assets.
